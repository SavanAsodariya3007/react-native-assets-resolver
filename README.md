# react-native-assets-resolver

An universal module to get information of Assets and pass them into other APIS

### Add the package to your npm dependencies

```
npm install react-native-assets-resolver
```